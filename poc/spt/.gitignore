# Local secrets — never commit real env (keep .env.example)
.env
.env.*
!.env.example

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Runtime data (runs, traces with JWTs, payload library)
data/

# Logs / local binaries
*.log
vendor/bin/
*.exe
