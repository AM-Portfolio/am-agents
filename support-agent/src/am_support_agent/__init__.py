"""AM Agent Platform — parallel A2A orchestration module (v2).

Does not import or replace legacy ``gateway/`` or ``platform_worker/``.
Specialist agents (tool/db/ui-test) are called only via HTTP adapters.
"""

__version__ = "0.1.1"
