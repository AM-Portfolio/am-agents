{
  "name": "@am/tool-agent",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "python -m uvicorn app.main:app --host 0.0.0.0 --port 8141 --reload",
    "test": "python -m pytest tests -q",
    "test:verbose": "python -m pytest tests -v",
    "test:preprod": "python scripts/test_preprod_backends.py"
  }
}
