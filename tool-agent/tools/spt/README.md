# SPT

Generic capability plugin. Backend name: `spt`.

Enable with `TOOL_AGENT_CAPABILITY_PLUGINS=spt` (or set `enabled: true`).

Provider selection: `SPT_PROVIDER` (default `memory`). Allowed: memory, k6.
