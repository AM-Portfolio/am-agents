# Mail

Generic capability plugin. Backend name: `mail`.

Enable with `TOOL_AGENT_CAPABILITY_PLUGINS=mail` (or set `enabled: true`).

Provider selection: `MAIL_PROVIDER` (default `memory`). Allowed: memory, zoho.
