# Document

Generic capability plugin. Backend name: `document`.

Enable with `TOOL_AGENT_CAPABILITY_PLUGINS=document` (or set `enabled: true`).

Provider selection: `DOCUMENT_PROVIDER` (default `memory`). Allowed: memory, minio.
