# Canonical KV v2 paths under mount `apps` (see VPS/vault/VAULT_SCHEMA.md).
defaults:
  mount: apps
  env: preprod
  path_prefix: data/preprod

read_allowlist:
  - preprod/
  - data/preprod/

write_allowlist:
  - preprod/infra/
  - preprod/services/

entities:
  postgres_infra:
    collection: preprod/infra/postgres
    description: Postgres connection credentials
  mongodb_infra:
    collection: preprod/infra/mongodb
    description: MongoDB connection credentials
  redis_infra:
    collection: preprod/infra/redis
    description: Redis connection credentials
  kafka_infra:
    collection: preprod/infra/kafka
    description: Kafka SASL credentials
  influxdb_infra:
    collection: preprod/infra/influxdb
    description: InfluxDB credentials
  shared_api_infra:
    collection: preprod/infra/shared-api
    description: Shared API keys (GitHub, SSL, Together)
  am_identity:
    collection: preprod/services/am-identity
    description: AM Identity OIDC / Keycloak client secrets
  am_mcp_gateway:
    collection: preprod/services/am-mcp-gateway
    description: MCP gateway observability and LLM API keys
  am_keycloak:
    collection: preprod/services/am-keycloak
    description: Keycloak admin and OIDC configuration
