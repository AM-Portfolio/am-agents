# Observe

Generic capability plugin. Backend name: `observe`.

Enable with `TOOL_AGENT_CAPABILITY_PLUGINS=observe` (or set `enabled: true`).

Provider selection: `OBSERVE_PROVIDER` (default `memory`). Allowed: memory, grafana.
