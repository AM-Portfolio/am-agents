# Directory

Generic capability plugin. Backend name: `directory`.

Enable with `TOOL_AGENT_CAPABILITY_PLUGINS=directory` (or set `enabled: true`).

Provider selection: `DIRECTORY_PROVIDER` (default `memory`). Allowed: memory, openproject.
