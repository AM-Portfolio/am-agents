# Work Item

Generic capability plugin. Backend name: `work-item`.

Enable with `TOOL_AGENT_CAPABILITY_PLUGINS=work-item` (or set `enabled: true`).

Provider selection: `WORK_ITEM_PROVIDER` (default `memory`). Allowed: memory, openproject.
