# Chat

Generic capability plugin. Backend name: `chat`.

Enable with `TOOL_AGENT_CAPABILITY_PLUGINS=chat` (or set `enabled: true`).

Provider selection: `CHAT_PROVIDER` (default `memory`). Allowed: memory, cliq.
