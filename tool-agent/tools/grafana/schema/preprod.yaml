defaults:
  start: now-1h
  end: now
  loki_datasource_uid: loki
  namespace: am-apps-preprod

entities:
  pod_logs:
    datasource_uid: ""
    logql_template: '{namespace="{namespace}", pod=~"{pod}.*"}'
    lookup_fields:
      namespace: namespace
      pod: pod
  namespace_logs:
    datasource_uid: ""
    logql_template: '{namespace="{namespace}"}'
    lookup_fields:
      namespace: namespace
