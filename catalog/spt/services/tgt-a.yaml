# Lab SPT catalog — opaque target ids only (no real prod service names in code)
id: tgt-a
kind: service
enabled: true
tags: [lab, api, tier-1]
prep_ref: prep.shared-lab
scenario_ref: scenario.smoke-get
query_ref: grafana.prom.error_rate
base_url_secret_ref: secret.lab.tgt-a.base_url
