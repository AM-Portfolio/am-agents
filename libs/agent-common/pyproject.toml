[project]
name = "agent-common"
version = "0.1.0"
description = "Shared OTel/HTTP helpers for AM agents (no vendor SDKs)"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/agent_common"]
