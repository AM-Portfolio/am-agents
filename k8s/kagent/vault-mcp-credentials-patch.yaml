# Placeholder — populate before applying vault-mcp-deployment.yaml
# kubectl create secret generic kagent-vault-mcp -n kagent \
#   --from-literal=VAULT_TOKEN='<token-with-read-policy>' \
#   --dry-run=client -o yaml | kubectl apply -f -
apiVersion: v1
kind: Secret
metadata:
  name: kagent-vault-mcp
  namespace: kagent
type: Opaque
stringData:
  VAULT_TOKEN: "REPLACE_ME"
